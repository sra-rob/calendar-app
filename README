CALENDAR-BACKEND
  1. Set environment variable "ENVIRONMENT_TYPE" to "dev"
  2. Run 
    - docker-compose -f "docker-compose.dev.yml" up -d
  3. Wait for mysql and redis to start
  4. Run 
    - cd calendar-backend && nohup ./mvnw spring-boot:run & cd ../

CALENDAR-FRONTEND
  1. Install npm
  2. Run 
    - cd calendar-frontend && npm i && nohup npm run dev & cd ../

POTENTIAL ERRORS
- If you are getting errors related to the ENVIRONMENT_TYPE environment variable you set at step 1 (CALENDAR-BACKEND), close vscode, intellij, terminal... and reopen it.
